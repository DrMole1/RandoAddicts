
<?php

// Définitions des constantes, celles ci sont disponibles sur les autres fichiers .php avec un include_once.

$url = "localhost"; // L'url de la db, ici on utilise localhost avec wamp
$user = "root"; // Le user de la db
$mdp = ""; // Le mot de passe de la db
$db = "randoaddict"; // Le nom de la db


?>
